# Vite build output — generated by `npm run build`, not source. The Dockerfile builds it
# fresh in its own build stage, and it isn't covered by the root .gitignore's dist rule
# (that one only covers /ui/shared/dist/).
/dist/
