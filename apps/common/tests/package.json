{
  "name": "@backup-manager/provider-conformance",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Cross-provider conformance suite (EPIC-B §63A). The one place in the repo that imports every apps/<provider>/frontend bridge, kept out of ui/shared so removing a provider app never breaks ui/shared's own build or test run.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit",
    "eslint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@testing-library/react": "^16.0.0",
    "@types/react": "^18.3.5",
    "@types/react-dom": "^18.3.0",
    "eslint": "^10.9.1",
    "jsdom": "^25.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "typescript": "^5.6.2",
    "typescript-eslint": "^8.68.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "@causlts/core": "0.3.3"
  }
}
