poll_interval: 15m

state:
  database: /var/lib/rclone-manager/state.db

sources:
  - id: production
    backup_sets:
      - id: postgres-primary
        remote:
          type: sftp
          host: production.example.internal
          user: backup
          key:
            command: ["/usr/local/bin/op", "read", "op://infra/rclone-manager/private-key"]
          known_hosts: /etc/rclone-manager/known_hosts
        remote_path: /backups/postgres
        local_path: /backups/production/postgres
        completion:
          strategy: stable
          stable_for: 10m
        stale_after: 30h
